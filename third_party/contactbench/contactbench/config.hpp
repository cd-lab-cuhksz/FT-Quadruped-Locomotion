/*
 * This file has been automatically generated by the jrl-cmakemodules.
 * Please see https://github.com/jrl-umi3218/jrl-cmakemodules/blob/master/config.hh.cmake for details.
*/

#ifndef CONTACTBENCH_CONFIG_HH
# define CONTACTBENCH_CONFIG_HH

// Package version (header).
# define CONTACTBENCH_VERSION_UNKNOWN_TAG 0 // Used to mention that the current version is unknown.
# define CONTACTBENCH_VERSION "0.0.0"
# define CONTACTBENCH_MAJOR_VERSION 0
# define CONTACTBENCH_MINOR_VERSION 0
# define CONTACTBENCH_PATCH_VERSION 0

#define CONTACTBENCH_VERSION_AT_LEAST(major, minor, patch) (CONTACTBENCH_MAJOR_VERSION>major || (CONTACTBENCH_MAJOR_VERSION>=major && \
                                                             (CONTACTBENCH_MINOR_VERSION>minor || (CONTACTBENCH_MINOR_VERSION>=minor && \
                                                                                                     CONTACTBENCH_PATCH_VERSION>=patch))))

#define CONTACTBENCH_VERSION_AT_MOST(major, minor, patch) (CONTACTBENCH_MAJOR_VERSION<major || (CONTACTBENCH_MAJOR_VERSION<=major && \
                                                            (CONTACTBENCH_MINOR_VERSION<minor || (CONTACTBENCH_MINOR_VERSION<=minor && \
                                                                                                     CONTACTBENCH_PATCH_VERSION<=patch))))

// Handle portable symbol export.
// Defining manually which symbol should be exported is required
// under Windows whether MinGW or MSVC is used.
//
// The headers then have to be able to work in two different modes:
// - dllexport when one is building the library,
// - dllimport for clients using the library.
//
// On Linux, set the visibility accordingly. If C++ symbol visibility
// is handled by the compiler, see: http://gcc.gnu.org/wiki/Visibility
//
// Explicit template instantiation on Windows need to add
// dllexport on the definition and dllimport on the declaration.
// The XXX_EXPLICIT_INSTANTIATION_DECLARATION_DLLAPI macro
// should be set on the declaration while
// the XXX_EXPLICIT_INSTANTIATION_DEFINITION_DLLAPI macro
// should be set on the definition.
# if defined _WIN32 || defined __CYGWIN__
// On Microsoft Windows, use dllimport and dllexport to tag symbols.
#  define CONTACTBENCH_DLLIMPORT __declspec(dllimport)
#  define CONTACTBENCH_DLLEXPORT __declspec(dllexport)
#  define CONTACTBENCH_DLLLOCAL
#  define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT __declspec(dllimport)
#  define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT
#  define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT __declspec(dllimport)
#  define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT __declspec(dllexport)
# else
// On Linux, for GCC >= 4, tag symbols using GCC extension.
#  if __GNUC__ >= 4
#   define CONTACTBENCH_DLLIMPORT __attribute__ ((visibility("default")))
#   define CONTACTBENCH_DLLEXPORT __attribute__ ((visibility("default")))
#   define CONTACTBENCH_DLLLOCAL  __attribute__ ((visibility("hidden")))
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT __attribute__ ((visibility("default")))
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT __attribute__ ((visibility("default")))
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT
#  else
// Otherwise (GCC < 4 or another compiler is used), export everything.
#   define CONTACTBENCH_DLLIMPORT
#   define CONTACTBENCH_DLLEXPORT
#   define CONTACTBENCH_DLLLOCAL
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT
#  endif // __GNUC__ >= 4
# endif // defined _WIN32 || defined __CYGWIN__

# ifdef CONTACTBENCH_STATIC
// If one is using the library statically, get rid of
// extra information and use standard explicit template
// instantiation keyword.
#  define CONTACTBENCH_DLLAPI
#  define CONTACTBENCH_LOCAL
#  define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION extern template
# else
// Depending on whether one is building or using the
// library define DLLAPI to import or export and
// define the right explicit template instantiation keyword.
#  ifdef contactbench_EXPORTS
#   define CONTACTBENCH_DLLAPI CONTACTBENCH_DLLEXPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLAPI CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLAPI CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT
#  else
#   define CONTACTBENCH_DLLAPI CONTACTBENCH_DLLIMPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLAPI CONTACTBENCH_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT
#   define CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLAPI CONTACTBENCH_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT
#  endif // CONTACTBENCH_EXPORTS
#  define CONTACTBENCH_LOCAL CONTACTBENCH_DLLLOCAL
# endif // CONTACTBENCH_STATIC
#endif //! CONTACTBENCH_CONFIG_HH
